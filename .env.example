# Supabase (production portal)
# Create a project at https://supabase.com, run supabase/schema.sql,
# then set these in Render → Environment and locally in .env

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
